import assert from "node:assert/strict";import test from "node:test";import {renderM9Ornaments} from "../../dist/renderer/m9-ornaments.js";import {renderM9FrostboundComponentSvg} from "../../dist/renderer/m9-frostbound-edge-components.js";
test("M9 ornaments remain deterministic, anchored, mirrored, and independent",()=>{const svg=renderM9Ornaments("m9-panel","panel",488,660);assert.match(svg,/data-ornament-layer="independent"/);assert.match(svg,/data-anchor="top-left"/);assert.match(svg,/data-mirror-of="m9-panel-corner-left"/);assert.equal(svg,renderM9Ornaments("m9-panel","panel",488,660));const panel=renderM9FrostboundComponentSvg({component:"panel",width:488,height:660});assert.match(panel,/data-ornament-id="m9-ice-corner"/);assert.match(panel,/data-safe-width="392"/);});
