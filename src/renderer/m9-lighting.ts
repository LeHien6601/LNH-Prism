export interface M9LightingModel{id:string;version:string;direction:"top";intensity:number;glow:number;}export const M9_FROSTBOUND_LIGHTING: M9LightingModel={id:"m9-cold-top-rim",version:"1.0.0",direction:"top",intensity:.72,glow:.28};export function renderM9LightingSvg(instanceId:string,width:number,height:number,model=M9_FROSTBOUND_LIGHTING):string{if(model.intensity<0||model.intensity>1||model.glow<0||model.glow>.5)throw new RangeError("Lighting values exceed the approved budget.");return `<g id="${instanceId}-lighting" data-lighting-model="${model.id}@${model.version}" data-light-direction="${model.direction}"><path d="M8 8H${width-8}" stroke="#E6FBFF" stroke-opacity="${model.intensity}"/><ellipse cx="${width/2}" cy="${height/2}" rx="${width*.3}" ry="${height*.2}" fill="#63CFF3" fill-opacity="${model.glow}"/></g>`;}
