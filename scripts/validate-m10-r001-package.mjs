import{createHash}from"node:crypto";import{readFile,access}from"node:fs/promises";import{resolve}from"node:path";const root=resolve("."),e=resolve(root,"docs/validation/evidence/m10-volcanic-forge"),m=JSON.parse(await readFile(resolve(e,"matrix.json"))),p=JSON.parse(await readFile(resolve(root,"assets/m10-volcanic-forge/manifest.json"))),h=x=>createHash("sha256").update(x).digest("hex");if(m.count!==26||p.modules.length!==52||!p.components.includes("icon-container"))throw Error("M10 matrix or canonical inventory incomplete");for(const x of m.entries){const s=await readFile(resolve(e,"matrix",`${x.name}.svg`));if(h(s)!==x.svgSha256||s.includes("<image")||!s.includes("m10-volcanic-forge-0.2.0"))throw Error(`M10 SVG receipt/boundary failure: ${x.name}`);if(x.component!=="progress"&&!s.includes("data-layer=\"forge-ornament\""))throw Error(`missing forge layer: ${x.name}`);if(x.component.includes("button")&&s.includes("data-ember-count=\"8\""))throw Error(`control emits embers: ${x.name}`);}for(const file of["M10-E-source-scale.html","M10-E-target-phone.html","M10-E-thumbnail.html","M10-R001-clean-reproduction-receipt.json","m10-volcanic-forge-target-phone.png"])await access(resolve(e,file));console.log(`validated ${m.count} M10 entries, ${p.modules.length} modules, canonical inventory, three review surfaces, and bounds.`);
